import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contato",
  description: "Contato",
}

const Contato = () => {
  return (
    <div>Contato</div>
  )
}

export default Contato;